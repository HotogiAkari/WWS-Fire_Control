# 战舰世界 AI 预瞄辅助工具技术蓝图 (WoWs-AI-Aim-Assist)

## 一、 系统架构与技术栈选型

整个系统将分为四个独立解耦的子模块，以确保高并发、低延迟。

1. **图像采集与解析层 (Sensor Layer)**
   * **技术栈**: DXGI (Windows高帧率截屏 API) / OpenCV / 预训练轻量化 OCR (如 PaddleOCR-Lite) / YOLOv8 (准星与小地图目标检测)
   * **功能**: 每秒 10-15 次捕获游戏画面，提取所有关键数值和图像特征。
2. **状态推演与AI预测层 (Brain Layer)**
   * **技术栈**: PyTorch / ONNX Runtime / LSTM (或 GRU) / 物理先验网络 (PINN)
   * **功能**: 接收历史帧序列，查询本地 SQLite/JSON 舰船数据库，输出未来 $t$ 秒的残差位移矢量。
3. **坐标映射与校准层 (Mapping Layer)**
   * **技术栈**: Numpy / Scipy
   * **功能**: 将预测的物理残差结合“动态准星刻度像素”转化为屏幕绝对像素坐标。
4. **可视化渲染层 (UI/Overlay Layer)**
   * **技术栈**: C++ / Python (PyQt / DearPyGui) / 穿透型透明窗口 (DirectX/OpenGL Overlay)
   * **功能**: 以 60/144 FPS 顺滑绘制预瞄圈、敌舰状态面板、轨迹预测线。

---

## 二、 核心算法管线详细设计

### 1. 相对残差模型 (Residual Lead Model) 的数学表达
由于 `X` 键锁定已经消化了敌舰的“当前线性运动”，模型只需计算：
* **预测位置** $P_{pred} = P_{t0} + \int_{0}^{t} \vec{V}(t) dt$
* **相机跟踪位置** $P_{cam} = P_{t0} + \vec{V}_{t0} \cdot t$
* **物理残差向量** $\Delta \vec{R} = P_{pred} - P_{cam}$
* **降维映射**：将 $\Delta \vec{R}$ 分解为垂直于视线的横向残差 $\Delta X$（映射到水平准星）和视线方向的纵深残差 $\Delta Y$（映射到垂直刻度）。

### 2. 动态准星刻度像素标定法 (Dynamic Crosshair Calibration)
这是本工程最巧妙的设计。无需计算相机的 FOV 和 3D-2D 投影矩阵：
* **实时标定**：通过 CV 检测屏幕画面中心到准星“10节”刻度线的像素距离，记为 $Pixel_{10}$。
* **单位节像素比**：$K = Pixel_{10} / 10$ （每节航速对应的像素跨度，随滚轮缩放自动变化）。
* **横向像素偏移**：$\Delta X_{pixel} = (\Delta X_{residual\_speed} \cdot \sin(\theta)) \cdot K$ （其中 $\theta$ 为敌舰与视线的夹角，由小地图解算）。
* **纵向像素偏移**：根据距离差 $\Delta D$，利用经验公式或多项式回归转化为 $\Delta Y_{pixel}$。

### 3. PINN / LSTM 意图预测网络
考虑到战舰世界中存在“插件、信号旗”导致的机动数据偏差，AI 模块的设计如下：
* **输入 (Input)**:
  * 连续时序特征（过去 $N$ 帧）：$X, Y, \phi$ (小地图坐标和船头角), $\omega$ (角速度), $a$ (加速度估计)。
  * 上下文特征：$D$ (距离), $t$ (炮弹飞行时间), 敌舰极限航速 $V_{max}$, 转向半径 $R$。
  * 状态特征（来自UI插件）：档位 (1/4, 2/4, Full, Reverse), 是否起火/进水。
* **网络结构**:
  * 第一层：全连接层进行特征对齐。
  * 第二层：LSTM/GRU 提取时序时空特征，预测玩家意图（如：“正在满舵减速”）。
  * 第三层：物理约束层（PINN）。将 LSTM 输出的加速度 $\alpha$ 限制在白板船数据库允许的范围内（加入 $\pm 15\%$ 的容差以兼容插件/旗子），输出最终的预测轨迹。

---

## 三、 工程实施细节与难点攻克

### 1. 多线程与数据同步 (Thread & Sync)
后台 15 FPS 与前台 60 FPS 存在严重的不同步问题，会导致预瞄点“瞬移、卡顿”。
* **解决方案**：引入**卡尔曼滤波 (Kalman Filter)** 或 **三次样条插值 (Cubic Spline Interpolation)**。
* 前台渲染线程维护一个运动学状态，后台 AI 线程每 66ms (15FPS) 更新一次目标点，前台在 66ms 内平滑过渡到新目标点，实现视觉上的极致顺滑。

### 2. 数据集构建与标注 (.wowsreplay 自动化工程)
监督学习需要海量的 Ground Truth (GT) 数据，手动标注是不现实的。
* **自动化采集方案**：
  1. 编写脚本解析 `.wowsreplay` 内存或通过 Python 自动化控制播放。
  2. 录制第一人称视角（隐藏UI以减少干扰，或者保留UI提取特征）。
  3. **GT 提取逻辑**：在 $t_0$ 时刻记录开火，快进到 $t_0 + t$ 时刻炮弹命中敌舰中心，记录此时敌舰中心相对于屏幕中心的像素偏移 $(\Delta X, \Delta Y)$，这就是 $t_0$ 时刻完美的 GT 预瞄点。
  4. 将 $t_0$ 时刻的小地图状态、飞行时间、准星标尺与 GT 打包成训练对 $(X_{input}, Y_{label})$。

### 3. 小地图与 UI OCR 的抗干扰
* 游戏内存在天气系统（暴风雪、气旋）、烟幕、水花特效，传统 CV 极易失效。
* **对策**：
  * 使用 YOLOv8 训练专门针对战舰世界小地图图标和 UI 数字的模型，比传统 Tesseract OCR 准确率高几个数量级。
  * 小地图坐标提取需要高精度，可以使用模板匹配 (Template Matching) 结合亚像素级 (Sub-pixel) 边缘检测来确定敌舰图标的精确中心点和偏航角。

---

## 四、 研发路线图 (Roadmap)

**Phase 1: 基础设施与数据采集 (Week 1-2)**
* 搭建 DXGI 高速截屏模块与 Overlay 透明绘制模块。
* 跑通基于小地图和动态准星的 CV 识别管线。
* 实现白板船静态数据库的 SQLite 查询。

**Phase 2: 物理残差模型与硬编码逻辑 (Week 3-4)**
* 暂不引入 AI 模型，仅使用物理公式和匀速/匀角速（CV/CTRV）模型，实现直线和简单转向的残差预测。
* 跑通“物理残差 -> 动态准星像素 -> 屏幕坐标”的映射。
* *此时你将获得一个能打匀速直线和简单匀速转弯敌舰的 Beta 版。*

**Phase 3: 数据集构建与 AI 训练 (Week 5-7)**
* 挂机解析 replay 录像，收集 10 万+ 级别的 (状态帧 -> 命中像素残差) 数据对。
* 构建并训练 LSTM 模型，学习玩家在加减速、骗炮、内切/外切时的操作惯性。

**Phase 4: 整合、联调与优化 (Week 8+)**
* 将 ONNX 格式的 AI 模型部署到预测线程。
* 优化多线程渲染平滑度，降低系统 CPU/GPU 占用率。
* 进行实战测试，调整模型权重和抗干扰能力。

```
WoWs-Aim-Assist/
│
├── main.py                    # 🟢 主程序入口：负责初始化各个模块，维护主循环 (Main Loop)
├── config.yaml                # ⚙️ 全局配置文件：存储屏幕分辨率、ROI区域坐标、颜色阈值等
│
├── data/                      # 🗄️ 数据资源目录 (不包含代码)
│   ├── static_ships.json      # 白板船只基础数据 (最大航速、转弯半径等)
│   └── models/                # 预留给后续 V2.0 机器学习模型 (如 .onnx 文件)
│
├── common/                    # 🛠️ 通用工具与数据结构 (Common Utilities)
│   ├── __init__.py
│   ├── data_models.py         # [核心] 定义 TargetState, TrajectoryPath 等数据类及存取函数
│   ├── config_loader.py       # 配置加载器：解析 config.yaml
│   ├── database_manager.py    # 数据库管理器：负责在内存中加载和查询 static_ships.json
│   └── logger.py              # 日志工具：统一终端输出格式，方便排错
│
├── recognition/               # 👁️ 图像识别模块 (Image Recognition)
│   ├── __init__.py
│   ├── capture.py             # 截屏工具：基于 DXGI 或 MSS 的高帧率屏幕抓取
│   ├── indicator_parser.py    # 状态解析器：用 OpenCV 处理档位进度条(n/4)和前进/倒车指示灯
│   ├── ocr_parser.py          # OCR解析器：提取飞行时间、距离、相对角度等数字
│   └── vision_manager.py      # [模块接口] 统筹截屏与解析，对外输出 TargetState 实例
│
├── prediction/                # 🧠 轨迹预测模块 (Prediction)
│   ├── __init__.py
│   ├── kinematics.py          # 基础物理引擎 (V1.0)：利用匀速/匀角速模型计算物理位移
│   ├── trajectory_builder.py  # 轨迹生成器：将预测结果封装为 TrajectoryPath 对象
│   ├── ai_predictor.py        # [预留] AI预测器 (V2.0)：加载 LSTM 模型推演非线性机动
│   └── prediction_manager.py  # [模块接口] 接收历史帧，调用引擎，输出最终的 TrajectoryPath
│
├── display/                   # 🎯 瞄准解算与显示模块 (Aiming & Display)
│   ├── __init__.py
│   ├── aiming_calculator.py   # 🧮 瞄点解算器：根据 TrajectoryPath 和当前飞行时间 t，计算出准确的物理交汇点 (dx, dy)
│   ├── crosshair_mapper.py    # 📐 坐标映射器：结合准星比例尺，将物理位移 (dx, dy) 转化为屏幕绝对像素坐标 (X, Y)
│   ├── overlay_window.py      # 🪟 透明窗口：基于 PyQt 或 DirectX 的无边框穿透窗口层
│   ├── renderer.py            # 🎨 渲染引擎：在计算出的 (X, Y) 坐标平滑绘制预瞄圈、轨迹线指示等
│   └── display_manager.py     # ⚙️ 模块接口：统筹解算、映射与渲染流水线
│
└── tools/                     # 🧰 独立工具箱 (开发辅助工具，不参与主循环)
    ├── ui_calibrator.py       # UI标定工具：首次运行时帮助用户框选UI，自动生成 ROI 坐标并写入 config.yaml
    └── data_recorder.py       # 数据录制工具：挂机回放时使用，收集 TargetState 保存为 JSON 用于 AI 训练
```